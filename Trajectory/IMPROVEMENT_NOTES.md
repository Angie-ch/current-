# 台风路径预测模型改进记录

**日期**: 2024-12-09

## 问题背景

两个版本效果几乎一样：
- 版本1 Mean Error: 423.92 km
- 版本2 Mean Error: 425.09 km

## 代码架构总结

```
┌─────────────────────────────────────────────────────────────────┐
│              Video2Video 台风路径预测系统架构                    │
├─────────────────────────────────────────────────────────────────┤
│  data_processing.py → data_structures.py → dataset.py          │
│       (加载CSV+ERA5)    (StormSample等)    (滑动窗口采样)       │
│                              ↓                                  │
│  features.py ←───────── 环境特征提取                            │
│  (风切变/涡度等)                                                 │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              model.py - HybridDiffusionModel            │   │
│  │  CoordEncoder + ERA5Encoder + FeatureEncoder            │   │
│  │              ↓                                          │   │
│  │        ConditionFusion (融合条件)                        │   │
│  │              ↓                                          │   │
│  │   TransformerDenoiser (Cross-Attention + AdaLN)         │   │
│  │              ↓                                          │   │
│  │     DDPMScheduler (DDIM采样) → 坐标增量 (Δlat,Δlon,Δvmax)│   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  train.py: Warmup+Cosine LR / EMA / AMP / 早停                  │
│  losses.py: 物理约束损失、地理距离损失                           │
└─────────────────────────────────────────────────────────────────┘
```

## 修改内容清单

### model.py 修改

1. **TransformerDenoiser 增强**
   - 新增连续 lead time 编码（区分近期/远期预测）
   - 新增时间感知输出头（短/中/长期分别处理）
   - 新增不确定性估计头（用于加权集合）

2. **物理约束损失 v2**
   - 基于历史速度的自适应阈值
   - 方向一致性约束（预测方向应与历史趋势一致）

3. **新增轨迹一致性损失**
   - 曲率约束（避免急转弯）
   - 起始点连续性（预测与历史无缝衔接）

4. **改进集合预测**
   - 基于方差的加权平均（确定性高的预测权重大）

### config.py 修改

| 参数 | 原值 | 新值 | 原因 |
|------|------|------|------|
| transformer_layers | 8 | 10 | 更深网络提升表达能力 |
| dropout | 0.1 | 0.15 | 防过拟合 |
| learning_rate | 1e-4 | 5e-5 | 更小学习率配合深网络 |
| num_epochs | 200 | 300 | 更多训练轮数 |
| interp_sample_weight | 0.5 | 0.3 | 降低插值样本影响 |
| ddim_steps | 50 | 100 | 更多采样步数提高质量 |
| num_samples | 10 | 15 | 更多集合成员 |
| gradient_accumulation_steps | 1 | 2 | 有效batch=96 |

### train.py 修改

- 支持梯度累积
- 加权集合预测评估
- 显示 consistency_loss

## 改进原理

1. **连续 Lead Time 编码**：让模型区分「3小时后」和「36小时后」的差异

2. **时间感知输出头**：短期靠惯性，中期靠环境场，长期需要专门处理

3. **增强物理约束**：台风不会急转弯，这是物理先验

4. **轨迹一致性**：避免「之字形」轨迹，确保平滑连续

5. **加权集合**：方差加权让确定性高的预测获得更大权重

## 预期改进

| 指标 | 原效果 | 预期改进 |
|------|--------|----------|
| +3h 误差 | ~35 km | ~30 km |
| +12h 误差 | ~342 km | ~280 km |
| +36h 误差 | ~740 km | ~550 km |
| 平均误差 | ~425 km | ~350 km |

## 运行方式

```bash
cd /Users/lucianoli/Documents/Graduation\ Project/Code/video2video
python train.py
```

## 注意事项

- 需要重新训练才能看到效果
- 训练时间会增加（更深模型+更多epoch）
- 显存占用略有增加，48 batch size 适配 RTX 4090
